language: python

python:
    - "2.7"
    - "3.6"
    - "3.7"
    - "3.8"
    - "pypy"
    - "pypy3"

sudo: false

env:
    - DJANGO=1.11
    - DJANGO=2.2
    - DJANGO=3.0
    - DJANGO=master

matrix:
    fast_finish: true
    exclude:
      - python: "2.7"
        env: DJANGO=master
      - python: "2.7"
        env: DJANGO=2.2
      - python: "2.7"
        env: DJANGO=3.0

    allow_failures:
      - env: DJANGO=master
      - python: "pypy"
      - python: "pypy3"

install:
    - pip install tox tox-travis

script:
    - tox

notifications:
    email: false
